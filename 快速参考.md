# TTQuant 快速参考卡

## 🚀 快速启动

### 启动策略引擎
```bash
cd Desktop/TTQuant
bash scripts/start-strategy-engine.sh
```

### 运行回测
```bash
bash scripts/run-backtest.sh
```

### 查看日志
```bash
docker logs -f ttquant-strategy-engine
```

### 访问监控
http://localhost:3000 (admin/admin123)

---

## 📝 配置文件

### 策略配置
`config/strategies.toml`

启用策略：
```toml
enabled = true  # 改为 true
```

### 交易模式
```toml
[global]
trading_mode = "paper"  # paper=模拟, live=实盘
```

---

## 🐳 Docker 命令

### 查看所有容器
```bash
docker ps
```

### 启动策略引擎
```bash
cd docker
docker-compose up -d strategy-engine
```

### 停止策略引擎
```bash
docker-compose stop strategy-engine
```

### 重启策略引擎
```bash
docker-compose restart strategy-engine
```

### 查看日志
```bash
docker logs -f ttquant-strategy-engine
```

### 运行回测
```bash
docker-compose --profile backtest up backtest
```

---

## 📊 监控面板

### Grafana
- URL: http://localhost:3000
- 用户: admin
- 密码: admin123

### 面板列表
1. OKX 实时数据
2. 回测结果
3. 系统监控

---

## 🔍 故障排查

### 策略引擎无法启动
```bash
# 检查行情服务
docker ps | grep md-okx

# 重新构建
cd docker
docker-compose build strategy-engine
docker-compose up -d strategy-engine
```

### 没有交易信号
```bash
# 检查策略是否启用
grep "enabled = true" config/strategies.toml

# 查看详细日志
docker logs -f ttquant-strategy-engine
```

### 回测失败
```bash
# 检查数据库
docker ps | grep timescaledb

# 检查数据量
docker exec ttquant-timescaledb psql -U ttquant -d ttquant_trading -c "SELECT COUNT(*) FROM market_data;"
```

---

## 📁 重要文件

### 文档
- `STRATEGY_ENGINE_GUIDE.md` - 详细使用指南
- `IMPLEMENTATION_COMPLETE.md` - 实施报告
- `VERIFICATION_CHECKLIST.md` - 验证清单
- `实施完成总结.md` - 中文总结

### 配置
- `config/strategies.toml` - 策略配置
- `config/markets.toml` - 市场配置
- `config/risk.toml` - 风控配置
- `.env` - 环境变量

### 脚本
- `scripts/start-strategy-engine.sh` - 启动引擎
- `scripts/run-backtest.sh` - 运行回测
- `系统管理面板.bat` - 管理面板

---

## 🎯 三种策略

### 1. 网格交易 (grid)
- 适合：震荡行情
- 原理：低买高卖
- 配置：`grid_trading_btc`

### 2. 均线交叉 (ma_cross)
- 适合：趋势行情
- 原理：金叉买入，死叉卖出
- 配置：`ma_cross_eth`

### 3. 动量突破 (momentum)
- 适合：突破行情
- 原理：动量突破 + 成交量确认
- 配置：`momentum_sol`

---

## ⚠️ 重要提示

### 模拟交易 vs 实盘
- 当前：模拟交易（安全）
- 切换实盘前：充分回测 + 小仓位测试

### 风控参数
- `stop_loss_percent` - 止损
- `take_profit_percent` - 止盈
- `max_position_usdt` - 持仓上限
- `max_daily_loss_usdt` - 日亏损限制

---

## 📞 获取帮助

1. 查看文档：`STRATEGY_ENGINE_GUIDE.md`
2. 查看日志：`docker logs -f ttquant-strategy-engine`
3. 运行管理面板：`系统管理面板.bat`

---

**项目状态**: ✅ 100% 完成
**当前模式**: 模拟交易
**数据量**: 25万+ 条
